/**
 * Static target URLs for tests that exercise external/reference sites rather
 * than the application under test (whose URL comes from Config.baseUrl).
 */
export const URLS = {
  DEMO: 'http://127.0.0.1:3000',
  WE_SEND_CV: 'https://wesendcv.com',
  EXAMPLE: 'https://example.com'
};
